<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="job">

  <select id="selectJobBoardOne" resultType="jobBoard" >
    SELECT * FROM BOARD_JOB WHERE 1=1 AND NO=#{no}
  </select>

  <select id="selectPageJobBoardList" resultType="map">
    SELECT J.*, (SELECT COUNT(*) FROM APPLY_JOB WHERE J.NO = MEMBERNO) AS APPLICANTS
     FROM BOARD_JOB J ORDER BY REGDATE DESC
  </select>

  <select id="selectJobBoardCount" resultType="_int">
    SELECT COUNT(*) FROM BOARD_JOB
  </select>

  <insert id="insertJobBoard" parameterType="jobBoard">
    insert into BOARD_JOB values(default, #{writer}, #{title}, #{content}, default, default, default)
  </insert>
</mapper>
